set(DEPS_INCLUDE_DIRS ${CMAKE_CURRENT_SOURCE_DIR})

list(APPEND DEPS_INCLUDE_DIRS "${CMAKE_CURRENT_SOURCE_DIR}/SDL/include")
list(APPEND DEPS_INCLUDE_DIRS "${CMAKE_CURRENT_SOURCE_DIR}/SDL_image")

set(DEPS_INCLUDE_DIRS ${DEPS_INCLUDE_DIRS} PARENT_SCOPE)
 